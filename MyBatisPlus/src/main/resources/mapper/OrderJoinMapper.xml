<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.mybatisplus.mapper.OrderJoinMapper">

    <select id="selectOrderUsersByStatus" resultType="com.github.mybatisplus.model.OrderUserDTO">
        select
            o.id as orderId,
            o.order_no as orderNo,
            o.amount as amount,
            o.status as orderStatus,
            u.id as userId,
            u.username as username,
            u.email as email
        from sys_order o
        left join sys_user u on o.user_id = u.id
        where o.status = #{status}
        order by o.id desc
    </select>

</mapper>

