<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.mybatisflex.mapper.OrderMapper">

    <select id="selectOrderUsersByStatus" resultType="com.github.mybatisflex.model.OrderUserDTO">
        select
            o.id as orderId,
            o.order_no as orderNo,
            o.amount as amount,
            o.status as orderStatus,
            u.id as userId,
            u.username as username,
            u.email as email
        from tb_order o
        left join tb_user u on o.user_id = u.id
        where o.status = #{status}
        order by o.id desc
    </select>

</mapper>

