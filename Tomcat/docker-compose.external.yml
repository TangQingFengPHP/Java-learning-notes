services:
  mysql:
    image: mysql:8.4
    container_name: tomcat-mysql-ext
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: tomcat_demo
      TZ: Asia/Shanghai
      LANG: C.UTF-8
    ports:
      - "3315:3306"
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_0900_ai_ci
    volumes:
      - mysql_data_ext:/var/lib/mysql
      - ./src/main/resources/db/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-uroot", "-p123456"]
      interval: 5s
      timeout: 3s
      retries: 30

  tomcat:
    build:
      context: .
      dockerfile: Dockerfile.external
    container_name: tomcat-app-external
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      DEPLOYMENT_MODE: external
      DB_URL: jdbc:mysql://mysql:3306/tomcat_demo?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_0900_ai_ci
      DB_USERNAME: root
      DB_PASSWORD: 123456
    ports:
      - "8188:8080"

volumes:
  mysql_data_ext:
